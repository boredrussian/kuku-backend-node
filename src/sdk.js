const { checkSignature, getHash } = require('./signatures')

// Don't have a dependency to DDB, React

// Methods:
// New source
// New post

// Private API:
// SRP Login (username, password, url)
// SRP Registration (source, username, password, url)
// Add post (post, token, url)
// Add source (source, token, )
// Subscribe (source, token, url)
// Unsubscribe (address, token, url)

// Public API:
// getSources()

