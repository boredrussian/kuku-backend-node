const {addInbox} = require('./addInbox');
const {getInbox} = require('./getInbox');
  const {updateInbox} = require('./updateInbox');


module.exports  = {
    addInbox,
    getInbox,
    updateInbox
}